
#version 400 core

layout (triangles) in;
layout (triangle_strip, max_vertices = 3) out;

//layout (location = 4) in mat4 instanceTransform;

uniform 

void main() {
    //gl_Position = instanceTransform*gl_in[0].gl_Position;
   // EmitVertex();

    //gl_Position = instanceTransform*gl_in[1].gl_Position;
   // EmitVertex();

   // gl_Position = instanceTransform*gl_in[2].gl_Position;
   // EmitVertex();
    
   // EndPrimitive();
}   